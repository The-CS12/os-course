---
# عنوان پست
title: "نحوه پردازش وقفه ها"
# تاریخ انتشار پست
date: 2026-06-20T09:48:01+03:30
# اگر فالس باشه نمایش داده نمیشه
draft: false
# اوردر پست بهتره به صورت اینکریمنتال افزایش پیدا کنه
order: 6
# توضیح کوتاه پست برای seo
description: "توی ترد قبل با وقفه ها آشنا شدیم اما توی این ترد به نحوه اجرا شدنش میپردازیم"
license: "CC BY-SA 4.0"
# تصویر شاخص پست فقط کافیه که نام تصویر رو بزارید
# اول تصویر رو در /static/assets/img قرار بدید
# سپس نام تصویر رو اینجا بگزارید مثلا x.jpeg

image: ""
# اگر میخوای این مطلب کامنت داشته باشه...
comments: true
---
____________________
#### وقفه ها اینجوری اجرا میشن❗    

شاید بگید مگه وقفه یه **سیگنال** نیست!؟‌ اجرا شدن داره مگه؟ باید بگم خود وقفه یه سیگناله ولی وقتی ارسال میشه CPU باید یه کار هایی بکنه دیگه! :) مراحل اجرای وقفه به ترتیب زیر انجام میشه:

1. وقفه توسط یکی از ۴ مورد که تو بیشتر بدونید ترد قبل گفتم تولید و ارسال میشه.
2. پردازنده اجرای برنامه (دستورالعمل) فعلی رو متوقف میکنه.
3. وقفه توسط پردازنده بررسی میشه. نوع و وجودش تایید میشه و در نهایت سیگنال تایید به منبع وقفه ارسال میشه که اجازه میده وقفه رو حذف کنه.
4. حالا پردازنده باید کنترل رو به **Interrupt routine** (یا `Interrupt handler`) منتقل کنه.<br>
برای شروع, اطلاعات دستورالعمل فعلی که تو رجیستر PSW ‍(یا `Program status word`) ذخیره شده و همچنین مقدار PC به علاوه تمامی اطلاعاتی که برای از سرگیری مجدد برنامه فعلی نیاز داره رو تو کنترل استک **(Control stack)** ذخیره میکنه.
5. بسته به نوع وقفه, سیستم عامل Interrupt handler اونو فراخوانی میکنه. Interrupt routine اجرا میشه.
6. بعد از اتمام کار handler و پردازش وقفه, اطلاعات برنامه که توی کنترل استک ذخیره شده بود به رجیستر ها برمیگرده و استک خالی میشه.
7. اجرای برنامه دقیقا از همون نقطه ای که متوقف شده بود از سر گرفته میشه :)


{{< readmore title="کنترل استک کجاست؟" type="info" open="false" >}}
 کنترل استک تو حافظه اصلی هستش (RAM) . اصولا هر جا نامی از استک هستش تو اکثر مواقع به رم اشاره میشه.
{{< /readmore >}}
{{< readmore title="اطلاعات داخل PSW دقیقا چیه؟" type="info" open="false" >}}
این اطلاعات خیلی زیاد و متنوع هستن ولی اصلی ترینشون شامل Flag ها,Execution mode ,IDE و ... هستن که همه اینها +‌ کد دستورالعمل به علاوه مقدار PC همگی به کنترل استک منتقل میشن.
{{< /readmore >}}
{{< readmore title="Interrupt handler دقیقا چیه؟" type="info" open="false" >}}
Interrupt handler یه برنامه سیستم عاملی هستش که داخل سیستم عامل وجود داره و وظیفش رسیدگی به وقفه و اجرای اونه. هر سیستمی بسته به معماریش میتونه یک یا چند تا handler داشته باشه❗
{{< /readmore >}}